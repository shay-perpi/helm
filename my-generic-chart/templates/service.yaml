apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-generic-chart.fullname" . }}
  labels:
    {{- include "my-generic-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-generic-chart.selectorLabels" . | nindent 4 }}
